# ระบบรายงานผลการดำเนินงานคณะทำงานย่อย

**สำนักงานคณะกรรมการข้าราชการตำรวจ (สบส.) — คำสั่งที่ 9/2569**

ระบบติดตามและรายงานความก้าวหน้าของคณะทำงานย่อยทั้ง 10 คณะ พร้อมหน้า Admin สำหรับแก้ไขข้อมูล

---

## 🖥️ คุณสมบัติ

- **หน้ารายงาน** (`/`) — แสดง Dashboard สถานะคณะทำงานทั้ง 10 คณะ, กรองตามสถานะ, แสดงรายละเอียดงาน
- **หน้า Admin** (`/admin`) — เข้าสู่ระบบเพื่อแก้ไขสถานะ, เปอร์เซ็นต์, ไทม์ไลน์, รายการงาน
- **REST API** — `/api/committees`, `/api/summary`
- **SQLite Database** — ข้อมูลถูกเก็บในไฟล์ ไม่ต้องติดตั้ง database server

---

## 🚀 วิธี Deploy

### ตัวเลือก A: Railway (แนะนำ — มี Backend + Admin)

**ขั้นตอน:**

1. Push โค้ดขึ้น GitHub repo

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/<your-username>/sbs-report.git
git push -u origin main
```

2. ไปที่ [railway.app](https://railway.app) → **New Project** → **Deploy from GitHub repo**

3. เลือก repo `sbs-report` → Railway จะ auto-detect Dockerfile

4. **สำคัญ**: เพิ่ม Volume สำหรับเก็บ database
   - ไปที่ Service Settings → **Add Volume**
   - Mount path: `/app/data`

5. Railway จะ deploy อัตโนมัติ เปิดดูที่ URL ที่ได้รับ

6. เข้า Admin: `https://<your-app>.railway.app/admin`

**ค่าเริ่มต้น Admin:**
- Username: `admin`
- Password: `sbs2569`

> ⚠️ **ควรเปลี่ยนรหัสผ่าน** หลัง deploy ครั้งแรก

---

### ตัวเลือก B: GitHub Pages (ฟรี — Static อย่างเดียว)

GitHub Pages รองรับเฉพาะ static files ไม่มี backend/admin

1. ใช้ไฟล์ `public/index.html` เพียงไฟล์เดียว
2. แก้ไขให้โหลดข้อมูลจาก static JSON แทน API:

```bash
# สร้างไฟล์ข้อมูล static
cp public/index.html docs/index.html
# แก้ fetch('/api/committees') เป็น fetch('data.json')
# สร้าง docs/data.json จากข้อมูลใน data/seed.js
```

3. ใน GitHub repo → Settings → Pages → Source: `main` branch, `/docs` folder

> **หมายเหตุ**: แบบ GitHub Pages จะต้องแก้ไขข้อมูลโดยตรงในไฟล์ `data.json` แล้ว push ขึ้น GitHub

---

## 🖥️ รันในเครื่อง (Local Development)

```bash
npm install
npm start
```

เปิด:
- Dashboard: http://localhost:3000
- Admin: http://localhost:3000/admin

---

## 📁 โครงสร้างโปรเจกต์

```
sbs-report/
├── server.js              # Express server + API
├── package.json
├── Dockerfile             # สำหรับ Railway
├── railway.toml           # Railway config + volume mount
├── data/
│   └── seed.js            # ข้อมูลเริ่มต้น 10 คณะ
└── public/
    ├── index.html          # หน้ารายงาน (Dashboard)
    └── admin.html          # หน้า Admin
```

## 🔌 API Endpoints

| Method | Path | คำอธิบาย |
|--------|------|----------|
| GET | `/api/committees` | ดูข้อมูลคณะทั้งหมด |
| GET | `/api/committees/:id` | ดูข้อมูลคณะเดียว |
| GET | `/api/summary` | สรุปจำนวนตามสถานะ |
| POST | `/api/auth/login` | เข้าสู่ระบบ |
| PUT | `/api/admin/committees/:id` | แก้ไขข้อมูลคณะ * |
| PUT | `/api/admin/committees/:id/tasks` | แก้ไขรายการงาน * |
| PUT | `/api/admin/password` | เปลี่ยนรหัสผ่าน * |

\* ต้องมี `Authorization: Basic <token>` header

---

## ⚙️ การตั้งค่า Environment Variables

| ตัวแปร | ค่าเริ่มต้น | คำอธิบาย |
|--------|-------------|----------|
| `PORT` | `3000` | Port ที่ server รัน |
| `DB_PATH` | `./data/sbs.db` | ตำแหน่งไฟล์ database |
